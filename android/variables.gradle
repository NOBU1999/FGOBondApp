ext {
    // 最低安卓版本：Android 8.0（API 26）—— 按 PLATFORM-ROADMAP 1.3 决定 3
    // Capacitor 默认 24，这里按项目决定调高到 26
    minSdkVersion = 26
    compileSdkVersion = 36
    targetSdkVersion = 36

    // 版本号单一来源：package.json（Windows 与安卓同一版本号）
    //   versionName = "0.1.10"（给人看）
    //   versionCode = 110（给系统看，必须单调递增：major*10000 + minor*100 + patch）
    def pkgJson = new groovy.json.JsonSlurper().parseText(file("../package.json").getText("UTF-8"))
    appVersionName = pkgJson.version
    def parts = appVersionName.tokenize(".").collect { it.replaceAll(/[^0-9].*$/, "") as int }
    while (parts.size() < 3) { parts << 0 }
    appVersionCode = parts[0] * 10000 + parts[1] * 100 + parts[2]
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
}